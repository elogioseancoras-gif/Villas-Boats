# Allow build scripts for specific trusted packages
# These packages need to compile native dependencies or download binaries
enable-pre-post-scripts=true

# Explicitly allow build scripts for these packages
# @swc/core - Fast JavaScript/TypeScript compiler (required by Next.js)
# sharp - Image optimization library (required by Next.js image optimization)
# unrs-resolver - Dependency resolver
